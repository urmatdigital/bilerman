# PRD: BILERMAN.KG v2

**Product Requirements Document**
**Версия**: 2.0
**Дата**: 2026-01-28
**Статус**: Draft

---

## 1. Ключевые изменения от v1

| Было (v1) | Стало (v2) |
|-----------|------------|
| OTP SMS аутентификация | Email + Password |
| Абстрактная БД | Neon PostgreSQL (конкретный инстанс) |
| Стандартный лендинг | Карта на главной (как 2GIS) |
| Admin роль | Staff роль (сотрудники платформы) |
| - | Автогенерация договоров для партнёров |

---

## 2. Архитектура

### 2.1 База данных
```
PostgreSQL на Neon:
postgresql://neondb_owner:***@ep-young-wildflower-agt7kso0-pooler.c-2.eu-central-1.aws.neon.tech/neondb
```

### 2.2 Стек
- **Frontend**: React 19 + Vite + TypeScript + Tailwind + PWA
- **Backend**: FastAPI + Python 3.11
- **Database**: Neon PostgreSQL
- **Cache**: Redis (Railway)
- **Maps**: Mapbox GL JS
- **Hosting**: Vercel (frontend) + Railway (backend)

---

## 3. Пользовательские роли

### 3.1 Клиент (Client)
Неавторизованный или авторизованный пользователь, ищущий жильё.

**Возможности:**
- Просмотр карты с объектами (без авторизации)
- Поиск и фильтрация объектов (без авторизации)
- Просмотр деталей объекта (без авторизации)
- Бронирование (требует авторизации)
- Оплата и управление бронированиями

### 3.2 Партнёр (Partner / Арендодатель)
Владелец недвижимости.

**Возможности:**
- Добавление и управление объектами
- Просмотр своих бронирований и клиентов
- Календарь доступности
- Настройки выплат (реквизиты банка/компании)
- Автоматическая генерация договоров
- Статистика доходов

### 3.3 Сотрудник (Staff)
Сотрудник платформы BILERMAN.KG.

**Возможности:**
- Просмотр всех партнёров и их объектов
- Просмотр всех клиентов
- Просмотр всех бронирований
- Модерация объектов
- Арбитраж споров
- Финансовые отчёты
- Фильтры: по компании, по объекту, по партнёру

---

## 4. MVP Приоритеты

### Фаза 1: Публичный сайт (неавторизованные пользователи)
**Приоритет: P0 — делаем первым**

1. **Главная страница = Карта**
   - Полноэкранная карта (Mapbox)
   - Автоопределение геолокации пользователя
   - Маркеры объектов на карте
   - Кластеризация при zoom out

2. **Поиск и фильтры**
   - Поиск по городу/району
   - Тип объекта: квартира, дом, коттедж, A-frame, комната
   - Даты заезда/выезда
   - Количество гостей
   - Диапазон цены
   - Удобства (WiFi, парковка, бассейн и т.д.)

3. **Список объектов**
   - Миниатюры с фото
   - Цена за ночь
   - Рейтинг
   - Основные удобства (иконки)
   - Переключение: карта / список

4. **Страница объекта**
   - Галерея фотографий
   - Описание, удобства
   - Карта с локацией
   - Календарь доступности
   - Калькулятор цены
   - Кнопка "Забронировать" (→ авторизация)

5. **PWA**
   - Установка на телефон
   - Офлайн-режим (базовый)
   - Push-уведомления (позже)

### Фаза 2: Авторизация и бронирование (клиенты)

1. **Регистрация/Вход по Email**
   - Email + пароль
   - Подтверждение email (опционально для MVP)
   - Восстановление пароля

2. **Профиль клиента**
   - Имя, телефон, email
   - История бронирований
   - Избранные объекты

3. **Бронирование**
   - Выбор дат
   - Оплата (эскроу)
   - Подтверждение

### Фаза 3: Кабинет партнёра

1. **Онбординг партнёра**
   - Регистрация как партнёр
   - Данные компании (ИНН, название, адрес)
   - Банковские реквизиты
   - Загрузка документов

2. **Автогенерация договоров**
   - Шаблон договора аренды
   - Подстановка данных партнёра и клиента
   - PDF генерация
   - Скачивание/отправка

3. **Dashboard партнёра**
   - Мои объекты
   - Мои бронирования
   - Мои клиенты (кто бронировал)
   - Баланс и выплаты
   - Календарь

### Фаза 4: Панель сотрудника

1. **Обзор платформы**
   - Все партнёры
   - Все объекты
   - Все клиенты
   - Все бронирования

2. **Фильтры и поиск**
   - По компании партнёра
   - По объекту
   - По региону
   - По статусу

3. **Модерация и арбитраж**

---

## 5. Функциональные требования

### 5.1 Модуль аутентификации (Email)

#### FR-AUTH-001: Регистрация
- Email (уникальный)
- Пароль (минимум 8 символов)
- Имя, фамилия
- Телефон (опционально)
- Роль по умолчанию: client

#### FR-AUTH-002: Вход
- Email + пароль
- JWT токены (access + refresh)
- "Запомнить меня" (30 дней)

#### FR-AUTH-003: Восстановление пароля
- Ссылка на email
- Срок действия 1 час

#### FR-AUTH-004: Роли
- `client` — клиент
- `partner` — партнёр (арендодатель)
- `staff` — сотрудник платформы

---

### 5.2 Модуль карты и поиска

#### FR-MAP-001: Главная страница
- Карта занимает 100% viewport
- Боковая панель с фильтрами (сворачиваемая)
- Список результатов (overlay или split view)

#### FR-MAP-002: Геолокация
- Запрос разрешения на геолокацию
- Центрирование карты на позиции пользователя
- Fallback: центр Бишкека

#### FR-MAP-003: Маркеры объектов
- Кастомные маркеры с ценой
- Popup при наведении (фото, название, цена)
- Клик → страница объекта
- Кластеризация

#### FR-MAP-004: Поиск по адресу
- Autocomplete (Mapbox Geocoding)
- Поиск по названию города/района
- Перемещение карты к результату

#### FR-MAP-005: Фильтры
```
- Город: Бишкек, Иссык-Куль, Каракол, Ош, Другое
- Тип: Квартира, Дом, Коттедж, A-frame, Комната, Хостел
- Даты: Check-in, Check-out
- Гости: 1-10+
- Цена: от X до Y сом/ночь
- Удобства: WiFi, Кондиционер, Парковка, Бассейн, Кухня, Стиралка
- Рейтинг: 4+, 4.5+
```

#### FR-MAP-006: Список объектов
- Карточки с миниатюрами (первое фото)
- Название, адрес, цена
- Рейтинг и количество отзывов
- Ключевые удобства (иконки)
- Сортировка: по цене, рейтингу, популярности

---

### 5.3 Модуль объектов

#### FR-PROP-001: Карточка объекта (превью)
```
┌─────────────────────────────────┐
│ [Фото]                          │
├─────────────────────────────────┤
│ Уютная квартира в центре        │
│ ★ 4.8 (24 отзыва)               │
│ 📍 Бишкек, ул. Московская       │
│ 👥 4 гостя · 🛏 2 спальни        │
│ ─────────────────────────────── │
│ 2,500 сом / ночь                │
└─────────────────────────────────┘
```

#### FR-PROP-002: Страница объекта
- Галерея (слайдер, fullscreen)
- Название, описание
- Удобства (иконки с подписями)
- Карта с точной локацией
- Календарь доступности
- Калькулятор цены
- Информация о партнёре
- Отзывы
- Кнопка "Забронировать"

#### FR-PROP-003: Создание объекта (партнёр)
- Мастер из нескольких шагов
- Step 1: Тип и адрес
- Step 2: Характеристики (комнаты, гости)
- Step 3: Удобства
- Step 4: Фотографии (5-20)
- Step 5: Цена и политика отмены
- Step 6: Описание (RU, KY)

---

### 5.4 Модуль партнёра

#### FR-PARTNER-001: Онбординг
**Шаг 1: Тип партнёра**
- Физическое лицо
- ИП / Самозанятый
- ОсОО / Компания

**Шаг 2: Данные компании/ИП**
- Название организации
- ИНН
- Юридический адрес
- Контактное лицо

**Шаг 3: Банковские реквизиты**
- Название банка
- БИК
- Расчётный счёт
- Или: MBank номер / Элсом кошелёк

**Шаг 4: Документы**
- Скан паспорта / удостоверения
- Свидетельство о регистрации (для ОсОО)
- Документы на собственность (опционально)

#### FR-PARTNER-002: Автогенерация договора
При каждом бронировании:
1. Берём шаблон договора
2. Подставляем данные партнёра (из онбординга)
3. Подставляем данные клиента (из профиля)
4. Подставляем данные бронирования (даты, сумма, объект)
5. Генерируем PDF
6. Сохраняем и отправляем обеим сторонам

**Шаблон договора включает:**
- Реквизиты сторон
- Предмет договора (объект, даты)
- Стоимость и порядок оплаты
- Права и обязанности
- Политика отмены
- Подписи (электронное согласие)

#### FR-PARTNER-003: Dashboard партнёра
```
┌─────────────────────────────────────────────┐
│ 👋 Добро пожаловать, [Имя]                  │
├─────────────────────────────────────────────┤
│ 💰 Баланс: 45,000 сом                       │
│ 📦 На эскроу: 12,000 сом                    │
│ [Вывести средства]                          │
├─────────────────────────────────────────────┤
│ 📊 Статистика за месяц                      │
│ • Бронирований: 8                           │
│ • Доход: 57,000 сом                         │
│ • Загрузка: 65%                             │
├─────────────────────────────────────────────┤
│ 🏠 Мои объекты (3)          [+ Добавить]    │
│ 📅 Календарь                                │
│ 📋 Бронирования                             │
│ 👥 Мои клиенты                              │
│ ⚙️ Настройки                                │
└─────────────────────────────────────────────┘
```

#### FR-PARTNER-004: Мои клиенты
Список всех клиентов, которые бронировали у партнёра:
- Имя, телефон, email
- История бронирований
- Общая сумма заказов
- Последнее бронирование

---

### 5.5 Модуль сотрудника (Staff)

#### FR-STAFF-001: Dashboard
- Общая статистика платформы
- Новые регистрации
- Активные бронирования
- Объекты на модерации
- Споры

#### FR-STAFF-002: Управление партнёрами
- Список всех партнёров
- Фильтр по компании
- Статус верификации
- Объекты партнёра
- Бронирования партнёра
- История выплат

#### FR-STAFF-003: Управление объектами
- Все объекты платформы
- Фильтр по партнёру, региону, типу
- Модерация (одобрить/отклонить)
- Редактирование

#### FR-STAFF-004: Управление клиентами
- Все клиенты
- История бронирований
- Связанные партнёры

#### FR-STAFF-005: Бронирования
- Все бронирования
- Фильтры: по дате, статусу, партнёру, объекту
- Детали бронирования
- Арбитраж споров

---

## 6. Нефункциональные требования

### 6.1 PWA
- Service Worker для офлайн
- Web App Manifest
- Установка на iOS/Android
- Иконки и splash screen

### 6.2 Responsive Design
- Mobile-first
- Breakpoints: 640px, 768px, 1024px, 1280px
- Touch-friendly UI

### 6.3 Производительность
- LCP < 2.5s
- FID < 100ms
- CLS < 0.1
- Lazy loading изображений

### 6.4 Безопасность
- HTTPS
- JWT с коротким сроком жизни
- CORS настройка
- Rate limiting
- SQL injection защита (ORM)
- XSS защита

---

## 7. База данных (Neon PostgreSQL)

### Основные таблицы

```sql
-- Пользователи
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    phone VARCHAR(20),
    role VARCHAR(20) DEFAULT 'client', -- client, partner, staff
    avatar_url TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Данные партнёра (для договоров)
CREATE TABLE partner_profiles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) UNIQUE,
    business_type VARCHAR(20), -- individual, ip, company
    company_name VARCHAR(255),
    inn VARCHAR(20),
    legal_address TEXT,
    contact_person VARCHAR(255),
    bank_name VARCHAR(255),
    bank_bik VARCHAR(20),
    bank_account VARCHAR(30),
    mbank_phone VARCHAR(20),
    elsom_wallet VARCHAR(50),
    documents JSONB, -- {passport: url, registration: url}
    is_verified BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Объекты недвижимости
CREATE TABLE properties (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    owner_id UUID REFERENCES users(id),
    title VARCHAR(255) NOT NULL,
    description TEXT,
    property_type VARCHAR(50), -- apartment, house, cottage, aframe, room, hostel
    address TEXT,
    city VARCHAR(100),
    latitude DECIMAL(10, 8),
    longitude DECIMAL(11, 8),
    max_guests INT,
    bedrooms INT,
    beds INT,
    bathrooms INT,
    amenities JSONB, -- ["wifi", "parking", "pool"]
    photos JSONB, -- ["url1", "url2"]
    price_per_night DECIMAL(10, 2),
    weekend_price DECIMAL(10, 2),
    min_nights INT DEFAULT 1,
    cancellation_policy VARCHAR(20) DEFAULT 'moderate',
    status VARCHAR(20) DEFAULT 'pending', -- pending, active, rejected, blocked
    rating DECIMAL(2, 1),
    reviews_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Бронирования
CREATE TABLE bookings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    property_id UUID REFERENCES properties(id),
    client_id UUID REFERENCES users(id),
    check_in DATE NOT NULL,
    check_out DATE NOT NULL,
    guests INT,
    total_price DECIMAL(10, 2),
    service_fee DECIMAL(10, 2),
    status VARCHAR(20) DEFAULT 'pending_payment',
    contract_url TEXT, -- PDF договора
    notes TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Платежи
CREATE TABLE payments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    booking_id UUID REFERENCES bookings(id),
    amount DECIMAL(10, 2),
    commission DECIMAL(10, 2),
    partner_amount DECIMAL(10, 2),
    method VARCHAR(20),
    status VARCHAR(20) DEFAULT 'pending',
    escrow_at TIMESTAMP,
    released_at TIMESTAMP,
    external_id VARCHAR(100),
    created_at TIMESTAMP DEFAULT NOW()
);

-- Выплаты партнёрам
CREATE TABLE payouts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    partner_id UUID REFERENCES users(id),
    amount DECIMAL(10, 2),
    method VARCHAR(20),
    status VARCHAR(20) DEFAULT 'pending',
    processed_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Отзывы
CREATE TABLE reviews (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    booking_id UUID REFERENCES bookings(id),
    property_id UUID REFERENCES properties(id),
    client_id UUID REFERENCES users(id),
    rating INT CHECK (rating >= 1 AND rating <= 5),
    comment TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Договоры
CREATE TABLE contracts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    booking_id UUID REFERENCES bookings(id) UNIQUE,
    partner_data JSONB, -- snapshot данных партнёра
    client_data JSONB, -- snapshot данных клиента
    booking_data JSONB, -- snapshot бронирования
    pdf_url TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Индексы
CREATE INDEX idx_properties_city ON properties(city);
CREATE INDEX idx_properties_type ON properties(property_type);
CREATE INDEX idx_properties_status ON properties(status);
CREATE INDEX idx_properties_location ON properties(latitude, longitude);
CREATE INDEX idx_bookings_property ON bookings(property_id);
CREATE INDEX idx_bookings_client ON bookings(client_id);
CREATE INDEX idx_bookings_dates ON bookings(check_in, check_out);
```

---

## 8. API Endpoints (основные)

### Public (без авторизации)
```
GET  /api/v1/properties              # Список объектов с фильтрами
GET  /api/v1/properties/{id}         # Детали объекта
GET  /api/v1/properties/{id}/calendar # Доступность
GET  /api/v1/cities                  # Список городов
```

### Auth
```
POST /api/v1/auth/register           # Регистрация
POST /api/v1/auth/login              # Вход
POST /api/v1/auth/refresh            # Обновить токен
POST /api/v1/auth/forgot-password    # Восстановление
POST /api/v1/auth/reset-password     # Сброс пароля
```

### Client
```
GET  /api/v1/me                      # Мой профиль
PUT  /api/v1/me                      # Обновить профиль
GET  /api/v1/me/bookings             # Мои бронирования
POST /api/v1/bookings                # Создать бронирование
GET  /api/v1/bookings/{id}           # Детали бронирования
POST /api/v1/bookings/{id}/cancel    # Отменить
```

### Partner
```
GET  /api/v1/partner/profile         # Профиль партнёра
PUT  /api/v1/partner/profile         # Обновить профиль
GET  /api/v1/partner/properties      # Мои объекты
POST /api/v1/partner/properties      # Создать объект
GET  /api/v1/partner/bookings        # Бронирования моих объектов
GET  /api/v1/partner/clients         # Мои клиенты
GET  /api/v1/partner/payouts         # Мои выплаты
POST /api/v1/partner/payouts/request # Запросить выплату
```

### Staff
```
GET  /api/v1/staff/dashboard         # Статистика
GET  /api/v1/staff/partners          # Все партнёры
GET  /api/v1/staff/properties        # Все объекты
GET  /api/v1/staff/clients           # Все клиенты
GET  /api/v1/staff/bookings          # Все бронирования
PUT  /api/v1/staff/properties/{id}/moderate # Модерация
```

---

## 9. UI/UX Wireframes

### Главная страница (Mobile)
```
┌─────────────────────────────┐
│ 🔍 Поиск по карте...    ☰  │
├─────────────────────────────┤
│                             │
│         [КАРТА]             │
│                             │
│    📍 маркеры объектов      │
│                             │
│                             │
├─────────────────────────────┤
│ ⬆️ Показать список (12)     │
└─────────────────────────────┘
```

### Главная страница (Desktop)
```
┌────────────────────────────────────────────────────────────────┐
│ 🏠 BILERMAN.KG    🔍 Поиск...    [Войти] [Стать партнёром]     │
├──────────────────────┬─────────────────────────────────────────┤
│                      │                                         │
│  Фильтры             │              [КАРТА]                    │
│  ─────────────       │                                         │
│  📍 Город            │         📍    📍                        │
│  🏠 Тип объекта      │              📍     📍                  │
│  📅 Даты             │                   📍                    │
│  👥 Гости            │        📍              📍               │
│  💰 Цена             │                                         │
│  ✨ Удобства         │                                         │
│                      │                                         │
│  [Применить]         │                                         │
│                      │                                         │
│  ─────────────       │                                         │
│  Результаты (24)     │                                         │
│                      │                                         │
│  ┌─────┐ Квартира    │                                         │
│  │ 📷  │ ★4.8        │                                         │
│  └─────┘ 2,500 сом   │                                         │
│                      │                                         │
│  ┌─────┐ Коттедж     │                                         │
│  │ 📷  │ ★4.5        │                                         │
│  └─────┘ 5,000 сом   │                                         │
│                      │                                         │
└──────────────────────┴─────────────────────────────────────────┘
```

---

## 10. Приоритизация (MoSCoW)

### Must Have (MVP Фаза 1):
- [x] Карта на главной странице
- [x] Геолокация
- [x] Список объектов с миниатюрами
- [x] Фильтры (город, тип, цена, даты)
- [x] Страница объекта
- [x] PWA (установка, offline shell)
- [x] Email авторизация
- [x] Создание бронирования

### Should Have (MVP Фаза 2-3):
- [ ] Профиль партнёра с реквизитами
- [ ] Автогенерация договоров
- [ ] Dashboard партнёра
- [ ] Платежи (эскроу)
- [ ] Панель сотрудника

### Could Have:
- [ ] Push-уведомления
- [ ] Отзывы и рейтинги
- [ ] Избранное
- [ ] Чат партнёр-клиент

### Won't Have (v1):
- [ ] OTP SMS
- [ ] Мобильные приложения (native)
- [ ] Интеграции с Booking/Airbnb

---

## 11. Acceptance Criteria

### AC-001: Карта загружается
```gherkin
Given пользователь открывает bilerman.kg
When страница загружена
Then отображается карта на весь экран
And запрашивается геолокация
And если разрешено — карта центрируется на пользователе
And если отказано — карта показывает Бишкек
```

### AC-002: Поиск объектов
```gherkin
Given пользователь на главной странице
When вводит "Иссык-Куль" в поиск
And выбирает даты 1-5 июля
And применяет фильтры
Then карта перемещается к Иссык-Кулю
And показываются маркеры доступных объектов
And список обновляется
```

### AC-003: Просмотр объекта
```gherkin
Given пользователь видит маркер на карте
When кликает на маркер
Then открывается страница объекта
And видны фото, описание, цена
And виден календарь доступности
And есть кнопка "Забронировать"
```

---

**Документ подготовлен**: 2026-01-28
**Версия**: 2.0
